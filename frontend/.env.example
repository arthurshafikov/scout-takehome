# Frontend API configuration
# VITE_API_URL is used to proxy API calls through nginx in Docker
# In development (Vite): calls go directly to backend
# In production (Docker): calls go through nginx proxy
VITE_API_URL=/api

# API key for backend authentication
# Same value as backend API_KEY in main.env
VITE_API_KEY=scout-api-key-12345
